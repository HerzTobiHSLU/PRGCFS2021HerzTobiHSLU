#include <stdio.h>

int main () { 
	int sum = 0;
	int start = 40;
	int end = 25;
	
	for( int i = start; i >= end; i-- )
	{ 
		printf("%d\n", i );
	}
	
	//printf("The sum from %d to %d is equal to %d\n", start, end, sum);
	
		
	return 0;
}
